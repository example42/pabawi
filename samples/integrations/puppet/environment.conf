# Pabawi Demo — Puppet Environment Configuration
# This file configures the Puppet environment directory layout.

modulepath = site-modules:modules:$basemodulepath
manifest  = manifests
hiera_config = hiera.yaml
environment_timeout = 0
