# Pabawi v1.0.0 - Modular Plugin Architecture Plan

**Status:** Draft for Review  
**Created:** 2026-01-31  
**Target Version:** 1.0.0

## Executive Summary

This plan describes a complete architectural refactoring of Pabawi to support a modular, extensible plugin system with:

- **Full-stack plugins** providing backend capabilities, frontend widgets, and CLI commands
- **Capability-based interface** replacing type-based plugin categories
- **RBAC (Role-Based Access Control)** enforced across Web UI, REST API, and CLI
- **Declarative YAML configuration** with environment variable overrides
- **Dynamic UI composition** with permission-aware plugin widgets
- **Extensible CLI** with auto-generated commands from plugin schemas
- **Debug mode** with correlation tracking across all interfaces
- **Database abstraction layer** for future database engine support
- **Plugin marketplace readiness** for third-party integrations

## Architecture Overview

### Three-Interface Platform

```
┌─────────────────────────────────────────────────────────┐
│                    Pabawi Platform                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐      │
│  │ Web UI   │      │ REST API │      │   CLI    │      │
│  │ (Svelte) │      │ (Express)│      │  (Node)  │      │
│  └────┬─────┘      └────┬─────┘      └────┬─────┘      │
│       │                 │                  │            │
│       └─────────────────┴──────────────────┘            │
│                         │                               │
│              ┌──────────▼──────────┐                    │
│              │ Plugin System       │                    │
│              │ - Capability Reg.   │                    │
│              │ - RBAC Engine       │                    │
│              │ - Widget Registry   │                    │
│              │ - Integration Mgr   │                    │
│              └──────────┬──────────┘                    │
│                         │                               │
│       ┌─────────────────┼─────────────────┐            │
│       │                 │                 │            │
│  ┌────▼────┐       ┌────▼────┐       ┌───▼────┐       │
│  │  Bolt   │       │PuppetDB │       │ Hiera  │       │
│  │ Plugin  │       │ Plugin  │       │ Plugin │       │
│  │         │       │         │       │        │       │
│  │Backend  │       │Backend  │       │Backend │       │
│  │Frontend │       │Frontend │       │Frontend│       │
│  │CLI Cmds │       │CLI Cmds │       │CLI Cmds│       │
│  └─────────┘       └─────────┘       └────────┘       │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  External Plugins (npm packages / local dirs)   │  │
│  │  @pabawi-plugins/ansible                        │  │
│  │  @pabawi-plugins/terraform                      │  │
│  │  backend/src/integrations/custom-plugin/        │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### Plugin Structure

Every plugin can provide:

1. **Backend Capabilities**: Functions that perform actions (execute commands, query data, compile configs)
2. **Frontend Widgets**: Svelte components that render in UI slots
3. **CLI Commands**: Command definitions with argument schemas

```
@pabawi-plugins/example/
├── package.json                   # Plugin metadata
├── backend/
│   └── index.ts                   # createPlugin() export
├── frontend/
│   ├── index.ts                   # Widget exports
│   ├── components/
│   │   ├── ExampleWidget.svelte
│   │   └── ExamplePanel.svelte
│   └── stores/
│       └── exampleState.svelte.ts
└── cli/
    └── commands.ts                # CLI command definitions (optional)
```

## Implementation Steps

### Phase 1: Core Plugin Infrastructure (Weeks 1-2)

#### Step 1: Define Enhanced Plugin Interfaces ✅ COMPLETED

**File:** `backend/src/integrations/types.ts`

**Status:** Completed 2026-01-31

**Actions Completed:**

- ✅ Kept deprecated `ExecutionToolPlugin` and `InformationSourcePlugin` interfaces (for backward compatibility, marked deprecated)
- ✅ Defined `PluginMetadata` interface (with additional fields: `integrationType`, `color`, `icon`, `minPabawiVersion`, `tags`):

  ```typescript
  interface PluginMetadata {
    name: string;
    version: string;
    author: string;
    description: string;
    homepage?: string;
    dependencies?: string[];        // Other plugin dependencies
    frontendEntryPoint?: string;    // Path to frontend bundle
    cliCommands?: PluginCLICommand[];
  }
  ```

- ✅ Defined `PluginCapability` interface (with `CapabilityCategory` type for standard categories):

  ```typescript
  interface PluginCapability {
    category: string;                // 'command', 'task', 'facts', 'config'
    name: string;                    // 'command.execute'
    description: string;
    handler: (params: any, context: ExecutionContext) => Promise<any>;
    requiredPermissions: string[];
    riskLevel: 'read' | 'write' | 'execute' | 'admin';
    schema?: CapabilitySchema;       // Zod schema for validation
  }
  
  interface CapabilitySchema {
    arguments: Record<string, ArgumentDefinition>;
    returns: {
      type: string;
      description: string;
      schema?: ZodSchema;
    };
  }
  
  interface ArgumentDefinition {
    type: 'string' | 'number' | 'boolean' | 'array' | 'object';
    description: string;
    required: boolean;
    default?: any;
    choices?: any[];
    validation?: ZodSchema;
  }
  ```

- ✅ Defined `PluginWidget` interface (with additional fields: `icon`, `priority`):

  ```typescript
  interface PluginWidget {
    id: string;                      // 'bolt:command-executor'
    name: string;
    component: string;               // Component path
    slots: WidgetSlot[];             // Where it can render
    size: 'small' | 'medium' | 'large' | 'full';
    requiredCapabilities: string[];  // For permission checks
    config?: Record<string, unknown>;
  }
  
  type WidgetSlot = 
    | 'dashboard' 
    | 'node-detail' 
    | 'inventory-panel' 
    | 'standalone-page';
  ```

- ✅ Defined `PluginCLICommand` interface:

  ```typescript
  interface PluginCLICommand {
    name: string;                    // 'bolt', 'puppetdb'
    actions: CLIAction[];
  }
  
  interface CLIAction {
    name: string;                    // 'run', 'query'
    capability: string;              // Maps to capability name
    description: string;
    aliases?: string[];
    examples?: string[];
  }
  ```

- ✅ Expanded `IntegrationType` enum (added `InstallSoftware`, `Info`):

  ```typescript
  enum IntegrationType {
    Provisioning = 'Provisioning',
    ConfigurationManagement = 'ConfigurationManagement',
    InventorySource = 'InventorySource',
    RemoteExecution = 'RemoteExecution',
    Monitoring = 'Monitoring',
    Orchestration = 'Orchestration',
    SecretManagement = 'SecretManagement', #pragma: allowlist secret
    ReportingAnalytics = 'ReportingAnalytics',
    AuditCompliance = 'AuditCompliance',
    BackupRecovery = 'BackupRecovery'
  }
  ```

- ✅ Defined unified `BasePluginInterface` (with optional `shutdown()` method):

  ```typescript
  interface BasePluginInterface {
    metadata: PluginMetadata;
    capabilities: PluginCapability[];
    widgets?: PluginWidget[];
    cliCommands?: PluginCLICommand[];
    configSchema?: ZodSchema;
    defaultPermissions?: Record<string, string[]>;  // capability -> roles
    
    initialize(): Promise<void>;
    healthCheck(): Promise<HealthCheckResult>;
    getConfig(): Record<string, unknown>;
    isInitialized(): boolean;
  }
  ```

#### Step 2: Build Capability Registry ✅ COMPLETED

**File:** `backend/src/integrations/CapabilityRegistry.ts`

**Status:** Completed 2026-01-31

**Actions Completed:**

- ✅ Created registry mapping capability names to plugin handlers (753 lines)
- ✅ Implemented methods:
  - `registerCapability(pluginName: string, capability: PluginCapability)`
  - `getProvidersForCapability(capabilityName: string): RegisteredCapability[]`
  - `getAllCapabilities(user?: User, options?): RegisteredCapability[]` (permission-filtered)
  - `executeCapability<T>(user: User, capabilityName: string, params: any, debugContext?: DebugContext): Promise<CapabilityExecutionResult<T>>`
  - `getPrimaryProvider(capabilityName: string): RegisteredCapability | undefined`
  - `unregisterPlugin(pluginName: string): number`
- ✅ Support priority-based routing when multiple plugins provide same capability
- ✅ Widget registration and capability linking:
  - `registerWidget(pluginName: string, widget: PluginWidget)`
  - `getAllWidgets(user?: User, slot?: string): RegisteredWidget[]`
  - `getWidgetsForCapability(capabilityName: string): RegisteredWidget[]`
- ✅ Helper methods: `getCapabilitiesByCategory()`, `getCapabilitiesByRiskLevel()`, `getStats()`

#### Step 3: Implement Plugin Loader ✅ COMPLETED

**File:** `backend/src/integrations/PluginLoader.ts`

**Status:** Completed 2026-01-31

**Actions Completed:**

- ✅ Scan directories: `backend/src/integrations/` and `node_modules/@pabawi-plugins/*/` (943 lines)
- ✅ Load plugins via dynamic `import()` with factory pattern support
- ✅ Validate plugin structure against interfaces (comprehensive validation)
- ✅ Resolve dependency graph with Kahn's algorithm (topological sort)
- ✅ Extract frontend metadata and CLI definitions
- ✅ Return loaded plugin instances with metadata

**Key Methods Implemented:**

- ✅ `async loadAll(): Promise<LoadedPlugin[]>` - Load all discovered plugins
- ✅ `async loadPlugin(path: string): Promise<LoadedPlugin | null>` - Load single plugin
- ✅ `validatePlugin(plugin: BasePluginInterface): PluginValidationResult` - Validate plugin structure
- ✅ `resolveDependencies(plugins: LoadedPlugin[]): LoadedPlugin[]` - Topological sort
- ✅ `async discover(): Promise<PluginDiscoveryResult[]>` - Discover plugins without loading
- ✅ `async reloadPlugin(name: string): Promise<LoadedPlugin | null>` - Hot-reload support
- ✅ `getPlugin(name: string): LoadedPlugin | undefined` - Get loaded plugin
- ✅ `getAllPlugins(): Map<string, LoadedPlugin>` - Get all loaded plugins

**Additional Features:**

- Plugin factory function support (`createPlugin()`, `default`, `Plugin` class)
- Circular dependency detection with error reporting
- Validation warnings for non-fatal issues
- Configurable strict validation mode
- Skip plugins configuration

#### Step 4: Refactor Integration Manager ✅ COMPLETED

**File:** `backend/src/integrations/IntegrationManager.ts`

**Status:** Completed 2026-01-31

**Breaking Changes (Implemented as Deprecations):**

- ✅ Marked `executionTools` and `informationSources` Maps as deprecated
- ✅ Legacy methods (`getExecutionTool`, `getInformationSource`, etc.) now log deprecation warnings
- ✅ Added capability-based routing via `executeCapability()`

**New Architecture (Implemented):**

- ✅ Uses `CapabilityRegistry` for all capability-based operations
- ✅ Uses `PluginLoader` for v1.0.0 plugin discovery and loading
- ✅ `initializePlugins()` now initializes both legacy and v1.0.0 plugins
- ✅ Added `getInventoryViaCapability()` for capability-based inventory access
- ✅ Added `getNodeFactsViaCapability()` for capability-based facts access

**Updated Methods:**

- ✅ `async initializePlugins(options?)`: Supports both legacy and v1.0.0 plugin loading
- ✅ `async executeCapability(user, capabilityName, params, debugContext)`: Routes to CapabilityRegistry
- ✅ `getPluginMetadata(pluginName)`: Returns plugin metadata
- ✅ `async reloadPlugin(pluginName)`: Hot-reload support

**Deprecated (will be removed in v2.0.0):**

- `getExecutionTool()` - Use `executeCapability('command.execute', ...)`
- `getInformationSource()` - Use `executeCapability('inventory.list', ...)`
- `getAllExecutionTools()` - Use `getCapabilitiesByCategory('command')`
- `getAllInformationSources()` - Use `getCapabilitiesByCategory('inventory')`

#### Step 5: Create Database Abstraction Layer ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `backend/src/database/interfaces/DatabaseInterface.ts` - Core interfaces
- ✅ `backend/src/database/adapters/SQLiteAdapter.ts` - SQLite implementation
- ✅ `backend/src/database/DatabaseFactory.ts` - Adapter factory
- ✅ `backend/src/database/index.ts` - Barrel exports
- 🔮 `backend/src/database/adapters/PostgreSQLAdapter.ts` (future)

**Actions Completed:**

- ✅ Defined `DatabaseAdapter` interface with connection, transaction, query, and schema methods
- ✅ Defined `DatabaseConfig` interface supporting SQLite, PostgreSQL, MySQL
- ✅ Implemented `SQLiteAdapter` with full interface compliance
- ✅ Created `DatabaseFactory` for adapter instantiation
- ✅ Updated `DatabaseService` to use adapter pattern (with backward compatibility)
- ✅ Added `DatabaseConfigSchema` to config/schema.ts
- ✅ Updated `AppConfigSchema` with optional `database` configuration
- ✅ Maintained backward compatibility with legacy `databasePath` string option
- ✅ `ExecutionRepository` continues to work via `getConnection()` deprecation bridge

**Breaking Changes:** None (fully backward compatible)

**New Capabilities:**

- Transaction support (`beginTransaction`, `commit`, `rollback`, `withTransaction`)
- Health checks (`healthCheck()`)
- Database dialect awareness (`getDialect()`)
- Migration framework with versioning
- Extensible adapter registration for plugins

**Database Interface Summary:**

```typescript
export interface DatabaseAdapter {
  // Connection management
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  isConnected(): boolean;
  
  // Transaction support
  beginTransaction(): Promise<void>;
  commit(): Promise<void>;
  rollback(): Promise<void>;
  
  // Query execution
  query<T>(sql: string, params?: any[]): Promise<T[]>;
  execute(sql: string, params?: any[]): Promise<{ changes: number; lastID?: number }>;
  
  // Schema management
  migrate(migrations: Migration[]): Promise<void>;
  getCurrentVersion(): Promise<number>;
  
  // Utility methods
  escape(value: any): string;
  getDialect(): 'sqlite' | 'postgresql' | 'mysql';
}

export interface Migration {
  version: number;
  name: string;
  up: string;
  down: string;
}

export interface DatabaseConfig {
  type: 'sqlite' | 'postgresql' | 'mysql';
  // SQLite
  path?: string;
  // PostgreSQL/MySQL
  host?: string;
  port?: number;
  database?: string;
  username?: string;
  password?: string;
  ssl?: boolean;
  // Common
  poolMin?: number;
  poolMax?: number;
  connectionTimeout?: number;
}
```

**SQLiteAdapter.ts** - Current implementation:

```typescript
export class SQLiteAdapter implements DatabaseAdapter {
  private db: Database | null = null;
  
  constructor(private config: DatabaseConfig) {}
  
  async connect(): Promise<void> {
    this.db = new Database(this.config.path);
    // Enable foreign keys, WAL mode, etc.
  }
  
  async query<T>(sql: string, params?: any[]): Promise<T[]> {
    return this.db.prepare(sql).all(params);
  }
  
  async execute(sql: string, params?: any[]): Promise<{ changes: number; lastID?: number }> {
    const result = this.db.prepare(sql).run(params);
    return { changes: result.changes, lastID: result.lastInsertRowid };
  }
  
  getDialect(): 'sqlite' { return 'sqlite'; }
  
  // ... other methods
}
```

**DatabaseFactory.ts** - Adapter selection:

```typescript
export class DatabaseFactory {
  static create(config: DatabaseConfig): DatabaseAdapter {
    switch (config.type) {
      case 'sqlite':
        return new SQLiteAdapter(config);
      case 'postgresql':
        // Future: return new PostgreSQLAdapter(config);
        throw new Error('PostgreSQL adapter not yet implemented');
      default:
        throw new Error(`Unsupported database type: ${config.type}`);
    }
  }
}
```

**Update DatabaseService.ts:**

```typescript
export class DatabaseService {
  private adapter: DatabaseAdapter;
  
  constructor(config: DatabaseConfig) {
    this.adapter = DatabaseFactory.create(config);
  }
  
  async initialize() {
    await this.adapter.connect();
    await this.runMigrations();
  }
  
  // Delegate all operations to adapter
  async query<T>(sql: string, params?: any[]): Promise<T[]> {
    return this.adapter.query<T>(sql, params);
  }
  
  // ... other methods
}
```

**Configuration support in schema.ts:**

```typescript
const DatabaseConfigSchema = z.object({
  type: z.enum(['sqlite', 'postgresql', 'mysql']).default('sqlite'),
  path: z.string().optional(),  // SQLite
  host: z.string().optional(),  // PostgreSQL/MySQL
  port: z.number().optional(),
  database: z.string().optional(),
  username: z.string().optional(),
  password: z.string().optional(),
  ssl: z.boolean().optional(),
  poolMin: z.number().default(2),
  poolMax: z.number().default(10),
});
```

**Benefits:**

- **Current:** SQLite works as-is with minimal changes
- **Future:** Easy to add PostgreSQL, MySQL, or other databases
- **Abstraction:** All database-specific code isolated in adapters
- **Migration:** Query builders can be database-aware (handle syntax differences)
- **Testing:** Mock adapter for unit tests

### Phase 2: Authentication & Authorization (Week 3)

#### Step 6: Create User Management System ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `backend/src/auth/types.ts` - Core type definitions
- ✅ `backend/src/auth/UserService.ts` - User account CRUD
- ✅ `backend/src/auth/GroupService.ts` - Group management CRUD
- ✅ `backend/src/auth/RoleService.ts` - Role and permission management
- ✅ `backend/src/auth/index.ts` - Barrel exports
- ✅ `backend/src/database/schema.sql` - Added auth tables

**Actions Completed:**

- ✅ Defined comprehensive type models:
  - `User`, `UserPublic`, `CreateUserInput`, `UpdateUserInput`
  - `Group`, `CreateGroupInput`, `UpdateGroupInput`
  - `Role`, `CreateRoleInput`, `UpdateRoleInput`
  - `Permission`, `PermissionAction`, `PermissionCondition`
  - `EffectivePermissions`, `PermissionCheckResult`
  - `PaginationOptions`, `PaginatedResult<T>` for list queries
  - Built-in role constants (`admin`, `operator`, `viewer`, `anonymous`)

- ✅ Implemented UserService with:
  - bcrypt password hashing (12 salt rounds)
  - CRUD operations (create, read, update, delete)
  - User lookup by ID, username, email
  - Password verification and last login tracking
  - Pagination and search support

- ✅ Implemented GroupService with:
  - CRUD operations for groups
  - Member management (add/remove users)
  - Role assignment to groups
  - Group lookup by ID and name
  - Pagination and search support

- ✅ Implemented RoleService with:
  - Built-in system roles initialization
  - CRUD operations (with protection for system roles)
  - Permission management (add/remove)
  - Capability pattern matching (`*`, `*.action`, exact match)
  - Effective roles calculation (user + group roles)

- ✅ Created database schema with:
  - `users` table with password hash, active flag, timestamps
  - `groups` table with name, description
  - `roles` table with priority, is_system flag
  - `permissions` table with capability patterns, conditions (JSON)
  - Junction tables: `user_groups`, `user_roles`, `group_roles`
  - Comprehensive indexes for efficient queries

- ✅ Added `bcrypt` dependency for password hashing

**Key Features:**

- **Priority-based role resolution:** Higher priority roles win in permission conflicts
- **Capability patterns:** Support for wildcards (`*`, `inventory.*`)
- **Conditional permissions:** Node filters, time windows, IP restrictions
- **Default permissions:** Built-in roles have sensible defaults
- **System role protection:** Cannot delete or rename system roles
- **Structured logging:** All operations logged with component/operation metadata

**Original Specification (for reference):**

- Define models:

  ```typescript
  interface User {
    id: string;
    username: string;
    email: string;
    passwordHash: string;
    groups: string[];
    roles: string[];
    active: boolean;
    createdAt: Date;
  }
  
  interface Group {
    id: string;
    name: string;
    description: string;
    roles: string[];
    members: string[];
    createdAt: Date;
  }
  
  interface Role {
    id: string;
    name: string;
    description: string;
    permissions: Permission[];
    priority: number;
    createdAt: Date;
  }
  
  interface Permission {
    capability: string;              // 'command.execute' or '*'
    action: 'allow' | 'deny';
    conditions?: PermissionCondition;
  }
  
  interface PermissionCondition {
    nodeFilter?: string;             // 'environment=dev'
    timeWindow?: string;
    ipRestriction?: string[];
  }
  ```

- Create database schema in `backend/src/database/schema.sql`:

  ```sql
  CREATE TABLE users (
    id TEXT PRIMARY KEY,
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    active INTEGER DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );
  
  CREATE TABLE groups (
    id TEXT PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    description TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );
  
  CREATE TABLE roles (
    id TEXT PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    description TEXT,
    priority INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );
  
  CREATE TABLE permissions (
    id TEXT PRIMARY KEY,
    role_id TEXT NOT NULL,
    capability TEXT NOT NULL,
    action TEXT NOT NULL,  -- 'allow' or 'deny'
    conditions TEXT,       -- JSON
    FOREIGN KEY (role_id) REFERENCES roles(id)
  );
  
  CREATE TABLE user_groups (
    user_id TEXT NOT NULL,
    group_id TEXT NOT NULL,
    PRIMARY KEY (user_id, group_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (group_id) REFERENCES groups(id)
  );
  
  CREATE TABLE user_roles (
    user_id TEXT NOT NULL,
    role_id TEXT NOT NULL,
    PRIMARY KEY (user_id, role_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (role_id) REFERENCES roles(id)
  );
  
  CREATE TABLE group_roles (
    group_id TEXT NOT NULL,
    role_id TEXT NOT NULL,
    PRIMARY KEY (group_id, role_id),
    FOREIGN KEY (group_id) REFERENCES groups(id),
    FOREIGN KEY (role_id) REFERENCES roles(id)
  );
  ```

- Implement services with bcrypt password hashing, CRUD operations

#### Step 7: Build Authentication Service ✅ COMPLETED

**Status:** Completed 2026-01-31

**File:** `backend/src/auth/AuthService.ts`

**Actions Completed:**

- ✅ Implemented JWT-based authentication with `jsonwebtoken` library
- ✅ Methods implemented:
  - `authenticate(username, password)` → `AuthTokens`
  - `validateToken(token)` → `ValidatedToken`
  - `refreshToken(refreshToken)` → `AuthTokens`
  - `revokeToken(token, reason?)` → void
  - `revokeAllUserTokens(userId, reason?)` → void
  - `generateTokens(user)` → `AuthTokens`
  - `getActiveSessionCount(userId)` → number
  - `cleanupExpiredTokens()` → number (for maintenance)

- ✅ Token structure implemented:

  ```typescript
  interface AuthTokens {
    accessToken: string;   // Short-lived (1 hour default)
    refreshToken: string;  // Long-lived (7 days default)
    tokenType: "Bearer";
    expiresIn: number;
    refreshExpiresIn: number;
  }

  interface JWTAccessPayload {
    type: "access";
    userId: string;
    username: string;
    roles: string[];
    groups: string[];
    jti: string;  // Token ID for revocation
    iat: number;
    exp: number;
  }
  ```

- ✅ Database tables for token management:
  - `refresh_tokens` - Tracks issued refresh tokens
  - `token_revocations` - Revocation list with expiry cleanup

- ✅ Security features:
  - Token revocation list with automatic cleanup
  - Refresh token rotation (old token revoked on refresh)
  - Account disabled check on validation
  - Configurable token expiry times

#### Step 8: Implement Authorization Service ✅ COMPLETED

**Status:** Completed 2026-01-31

**File:** `backend/src/auth/AuthorizationService.ts`

**Actions Completed:**

- ✅ Core method: `checkPermission(user, capability, context?)` → `PermissionCheckResult`
- ✅ Algorithm implemented:
  1. Resolve user's effective roles (direct + via groups)
  2. Collect all permissions from those roles
  3. Sort by priority (deny > allow, specific > wildcard)
  4. Evaluate conditions (node filters, time windows, IP restrictions)
  5. Return true if allowed, false otherwise

- ✅ Helper methods implemented:
  - `getEffectivePermissions(user)` → `EffectivePermissions`
  - `checkWidgetAccess(user, requiredCapabilities)` → `WidgetAccessResult`
  - `filterCapabilitiesByPermissions(capabilities, user)` → filtered capabilities
  - `invalidateCache(userId)` / `invalidateAllCaches()` - Cache management

- ✅ Permission caching with configurable TTL (default: 5 minutes)
- ✅ Cache invalidation on role/permission changes

- ✅ Capability pattern matching:
  - `*` matches everything
  - `category.*` matches all capabilities in category
  - `category.action` exact match

- ✅ Conditional permission evaluation:
  - Node filters: `environment=production`, `role=webserver`, `name~web*`
  - Time windows: `weekdays:09:00-17:00`, `weekend:00:00-23:59`
  - IP restrictions: exact match, CIDR ranges, wildcards

#### Step 9: Add Authentication Middleware ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `backend/src/middleware/auth.ts` - JWT authentication middleware
- ✅ `backend/src/middleware/rbac.ts` - RBAC authorization middleware

**auth.ts Features:**

- `createAuthMiddleware(config)` - Factory for auth middleware
- `requireAuth(config)` - Shorthand for required authentication
- `optionalAuth(config)` - For routes with optional auth
- Extracts Bearer token from Authorization header
- Validates token and attaches user to request
- Extends Express Request with `user` and `isAuthenticated` properties

**rbac.ts Features:**

- `initializeRbac(config)` - Initialize shared authorization service
- `requireCapability(capability, options)` - Single capability check
- `requireAnyCapability(capabilities, options)` - Any of multiple capabilities
- `requireAllCapabilities(capabilities, options)` - All capabilities required
- `requireAdmin(options)` - Shorthand for admin access
- Automatic execution context extraction from request
- Node/target detection from params, query, or body
- Cache invalidation helpers

#### Step 10: Create Auth Routes ✅ COMPLETED

**Status:** Completed 2026-01-31

**File:** `backend/src/routes/auth.ts`

**Endpoints Implemented:**

- ✅ `POST /api/auth/login` - Username/password → tokens + user
- ✅ `POST /api/auth/refresh` - Refresh token → new tokens
- ✅ `POST /api/auth/logout` - Revoke tokens (single or all sessions)
- ✅ `GET /api/auth/me` - Current user + effective permissions
- ✅ `GET /api/auth/sessions` - Active session count
- ✅ `POST /api/auth/check` - Check specific capability permission

**Features:**

- Expert mode support with debug info attachment
- Structured logging for all operations
- Proper error handling with specific error codes
- Input validation

#### Step 11: Create RBAC Management Routes ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `backend/src/routes/users.ts` - User management routes
- ✅ `backend/src/routes/groups.ts` - Group management routes
- ✅ `backend/src/routes/roles.ts` - Role management routes

**Actions Completed:**

- ✅ Implemented CRUD endpoints for users, groups, roles
- ✅ All routes require admin role via `requireAdmin()` middleware
- ✅ Expert mode support with debug info attachment
- ✅ Structured logging for all operations
- ✅ Proper error handling with specific error codes
- ✅ Cache invalidation on permission-affecting changes

**User Routes (`/api/users`):**

- `GET /` - List users (paginated)
- `GET /search` - Search users by username/email/displayName
- `GET /:id` - Get user by ID
- `POST /` - Create user
- `PUT /:id` - Update user
- `DELETE /:id` - Delete user (prevents self-deletion)
- `GET /:id/groups` - Get user's groups
- `GET /:id/roles` - Get user's effective roles

**Group Routes (`/api/groups`):**

- `GET /` - List groups (paginated)
- `GET /search` - Search groups
- `GET /:id` - Get group by ID
- `POST /` - Create group
- `PUT /:id` - Update group
- `DELETE /:id` - Delete group
- `GET /:id/members` - Get group members
- `GET /:id/roles` - Get group roles
- `POST /:id/members/:userId` - Add member to group
- `DELETE /:id/members/:userId` - Remove member from group
- `POST /:id/roles/:roleId` - Add role to group
- `DELETE /:id/roles/:roleId` - Remove role from group

**Role Routes (`/api/roles`):**

- `GET /` - List roles (paginated)
- `GET /search` - Search roles
- `GET /:id` - Get role by ID
- `POST /` - Create role
- `PUT /:id` - Update role (protects system roles)
- `DELETE /:id` - Delete role (prevents system role deletion)
- `POST /:id/permissions` - Add permission to role
- `DELETE /:id/permissions/:capability` - Remove permission from role
- `GET /:id/users` - Get users with this role
- `GET /:id/groups` - Get groups with this role
- `POST /initialize` - Initialize built-in system roles

### Phase 3: Configuration System (Week 4)

#### Step 12: Build YAML Configuration Support ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `backend/src/config/YamlConfigLoader.ts` - YAML parsing and env var interpolation
- ✅ `backend/src/config/index.ts` - Barrel exports for config module
- ✅ `backend/config/integrations.yaml.example` - Integration configuration template
- ✅ `backend/config/rbac.yaml.example` - RBAC configuration template
- ✅ `backend/config/database.yaml.example` - Database configuration template
- ✅ `backend/test/unit/YamlConfigLoader.test.ts` - Unit tests (24 passing)

**Actions Completed:**

- ✅ Added YAML parsing with `yaml` package (already installed)
- ✅ Created `YamlConfigLoader` class with comprehensive features:
  - `loadFile<T>()` - Load and parse YAML with type safety
  - `loadFileOrEmpty()` - Load with fallback to empty object
  - `loadAndMerge()` - Merge multiple config files (later overrides earlier)
  - `loadFirst()` - Load first existing file from list
  - `fileExists()` / `getResolvedPath()` - Path utilities
  - `writeFile()` - Write config with optional comment header

- ✅ Implemented environment variable interpolation:
  - `${VAR_NAME}` - Required variable (placeholder preserved if missing)
  - `${VAR_NAME:-default}` - Variable with default value
  - `${VAR_NAME:?error message}` - Custom error if missing (throws)
  - Supports nested interpolation in objects and arrays
  - Tracks interpolated and missing variables

- ✅ Integrated with `ConfigService`:
  - `loadYamlConfigs()` - Loads integrations, rbac, database configs
  - `getYamlConfig()` - Returns loaded YAML configuration
  - `getRbacConfig()` - Returns RBAC configuration from YAML
  - `hasYamlConfig()` - Check if specific config was loaded
  - `reloadYamlConfigs()` - Hot-reload configuration files

- ✅ Configuration precedence implemented: CLI args > env vars > YAML > defaults

**Default Config File Paths:**

```typescript
DEFAULT_CONFIG_PATHS = {
  integrations: ["config/integrations.yaml", "config/integrations.yml", ...],
  rbac: ["config/rbac.yaml", "config/rbac.yml", ...],
  database: ["config/database.yaml", "config/database.yml", ...],
  main: ["config/pabawi.yaml", "config/pabawi.yml", ...],
}
```

**Example YAML Files Created:**

- `integrations.yaml.example` - Full Bolt, PuppetDB, Puppetserver, Hiera config
- `rbac.yaml.example` - Auth settings, role definitions, permission templates
- `database.yaml.example` - SQLite (default), PostgreSQL and MySQL templates

#### Step 13: Create Configuration Schemas ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `backend/src/config/SchemaRegistry.ts` - Schema registry for plugin configurations
- ✅ `backend/src/config/YamlConfigSchemas.ts` - Comprehensive YAML validation schemas
- ✅ `backend/test/unit/SchemaRegistry.test.ts` - Unit tests (24 tests)
- ✅ `backend/test/unit/YamlConfigSchemas.test.ts` - Unit tests (37 tests)
- ✅ Updated `backend/src/config/index.ts` - Barrel exports

**Actions Completed:**

- ✅ Created `SchemaRegistry` class for centralized schema management:
  - Singleton pattern with `getSchemaRegistry()` / `resetSchemaRegistry()`
  - Schema registration with metadata (id, name, description, version, category)
  - Per-plugin schema registration with `registerPluginSchema()`
  - Core schema registration with `registerCoreSchema()`
  - Schema retrieval by ID, category, plugin name
  - Schema validation with detailed error reporting
  - Schema composition for merging multiple schemas
  - Plugin schema unregistration (`unregisterPlugin()`)
  - Statistics and metadata export for documentation

- ✅ Defined Zod schemas for `integrations.yaml` validation:
  - `IntegrationsYamlSchema` - Complete integrations.yaml structure
  - `YamlBoltIntegrationSchema` - Bolt plugin configuration
  - `YamlPuppetDBIntegrationSchema` - PuppetDB plugin configuration
  - `YamlPuppetserverIntegrationSchema` - Puppetserver plugin configuration
  - `YamlHieraIntegrationSchema` - Hiera plugin configuration
  - `YamlExternalPluginSchema` - External/third-party plugin configuration
  - `YamlWidgetConfigSchema` - Frontend widget configuration
  - `YamlFrontendConfigSchema` - Frontend integration configuration
  - `DefaultPermissionsSchema` - Capability to roles mapping

- ✅ Defined Zod schemas for `rbac.yaml` validation:
  - `RbacYamlSchema` - Complete rbac.yaml structure
  - `YamlAuthConfigSchema` - Authentication configuration
  - `YamlJWTConfigSchema` - JWT token settings
  - `YamlSessionConfigSchema` - Session management settings
  - `YamlPasswordPolicySchema` - Password policy rules
  - `YamlRoleSchema` - Role definition with permissions
  - `YamlPermissionSchema` - Permission with capability pattern and conditions
  - `YamlPermissionConditionSchema` - Node filters, time windows, IP restrictions
  - `YamlGroupSchema` - Group definition with roles
  - `YamlUserSchema` - User seed data with email validation

- ✅ Defined Zod schemas for `database.yaml` validation:
  - `DatabaseYamlSchema` - Complete database.yaml structure
  - `YamlMigrationConfigSchema` - Migration settings (autoRun, directory)
  - `YamlBackupConfigSchema` - Backup settings (schedule, retention)

- ✅ Created combined schema `PabawiYamlConfigSchema` for all YAML configs

- ✅ Implemented validation helpers:
  - `validateIntegrationsYaml(data)` - Validate integrations config
  - `validateRbacYaml(data)` - Validate RBAC config
  - `validateDatabaseYaml(data)` - Validate database config
  - `validatePabawiConfig(data)` - Validate combined config

- ✅ Implemented schema registration function:
  - `registerYamlConfigSchemas()` - Register all YAML schemas in registry

- ✅ Updated barrel exports with all new types and functions

**Test Results:** 61 tests passing (24 SchemaRegistry + 37 YamlConfigSchemas)

**Schema Categories:**

| Category | Description | Schemas |
|----------|-------------|---------|
| `core` | Core application schemas | `yaml:pabawi` |
| `integration` | Integration plugin configs | `yaml:integrations`, `yaml:integrations:bolt`, `yaml:integrations:puppetdb`, `yaml:integrations:puppetserver`, `yaml:integrations:hiera` |
| `auth` | Authentication/RBAC | `yaml:rbac`, `yaml:rbac:auth`, `yaml:rbac:role`, `yaml:rbac:permission` |
| `database` | Database configuration | `yaml:database` |
| `plugin` | Plugin-specific schemas | (registered by plugins) |
| `custom` | User-defined schemas | (runtime registration) |

**Usage Example:**

```typescript
import {
  validateIntegrationsYaml,
  registerPluginSchema,
  getSchemaRegistry,
} from './config';

// Validate YAML config
const result = validateIntegrationsYaml(loadedConfig);
if (!result.success) {
  console.error('Invalid config:', result.error.format());
}

// Register plugin-specific schema
registerPluginSchema('my-plugin', mySchema, {
  id: 'plugin:my:config',
  name: 'My Plugin Config',
  description: 'Configuration for my plugin',
  version: '1.0.0',
  category: 'plugin',
});

// Validate using registry
const registry = getSchemaRegistry();
const validation = registry.validate('plugin:my:config', data);
```

#### Step 14: Create Example YAML Configs ✅ COMPLETED (merged into Step 12)

**Status:** Completed as part of Step 12

**Files Created (see Step 12 for details):**

- ✅ `backend/config/integrations.yaml.example`
- ✅ `backend/config/rbac.yaml.example`
- ✅ `backend/config/database.yaml.example`

### Phase 4: Frontend Plugin System (Weeks 5-6)

#### Step 15: Create Frontend Plugin Loader ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `frontend/src/lib/plugins/types.ts` - Frontend plugin type definitions
- ✅ `frontend/src/lib/plugins/PluginLoader.ts` - Plugin loading and management
- ✅ `frontend/src/lib/plugins/index.ts` - Barrel exports

**Actions Completed:**

- ✅ Created comprehensive type definitions (`types.ts`):
  - `PluginWidget`, `LoadedWidget`, `WidgetLoadState` - Widget types
  - `PluginInfo`, `LoadedPlugin`, `PluginLoadState` - Plugin types
  - `PluginMetadata`, `PluginCapabilitySummary` - Metadata types
  - `PluginFrontendModule` - Module export interface
  - `PluginEvent`, `PluginEventHandler` - Event types
  - `PluginLoaderConfig`, `DEFAULT_PLUGIN_LOADER_CONFIG` - Configuration
  - `WidgetValidationResult`, `PluginValidationResult` - Validation types
  - `IntegrationType` enum mirroring backend

- ✅ Implemented `PluginLoader` class with:
  - `loadAll()` - Load all available plugins from backend API
  - `loadPlugin(name)` - Load a specific plugin by name
  - `reloadPlugin(name)` - Reload plugin with fresh metadata
  - `unloadPlugin(name)` - Unload and cleanup plugin
  - `getPlugin(name)` / `getAllPlugins()` - Retrieve loaded plugins
  - `getWidgetsForSlot(slot, userCapabilities?)` - Get widgets for a slot with permission filtering
  - `getWidgetComponent(widgetId)` - Get cached widget component
  - `loadWidgetComponent(widgetId)` - Lazy load widget component
  - `validatePlugin(info)` - Validate plugin and widget definitions
  - `subscribe(handler)` - Subscribe to plugin lifecycle events
  - `clearAll()` - Clear all caches and cleanup

- ✅ Dynamic import system features:
  - Lazy widget loading (configurable)
  - Plugin module loading with initialization hooks
  - Retry logic with exponential backoff
  - Component caching for performance

- ✅ Error handling:
  - Graceful degradation on load failures
  - Partial load states for plugins with some failed widgets
  - Detailed error messages in LoadedWidget/LoadedPlugin
  - Event emission for error tracking

- ✅ Validation:
  - Widget ID format validation (pluginName:widgetName)
  - Required field checks (id, name, component, slots)
  - Slot name validation
  - Warnings for non-fatal issues

- ✅ Caching:
  - Plugin metadata caching with configurable TTL
  - Widget component caching
  - Plugin module caching
  - Request deduplication for concurrent loads

- ✅ Singleton pattern with `getPluginLoader()` / `resetPluginLoader()`

**Configuration Options:**

```typescript
interface PluginLoaderConfig {
  apiBaseUrl: string;        // Default: "/api"
  assetsBaseUrl: string;     // Default: "/plugins"
  lazyLoadWidgets: boolean;  // Default: true
  metadataCacheTTL: number;  // Default: 60000 (1 minute)
  maxRetries: number;        // Default: 3
  retryDelay: number;        // Default: 1000ms
  debug: boolean;            // Default: false
}
```

**Usage Example:**

```typescript
import { getPluginLoader } from '$lib/plugins';

// Get the plugin loader
const loader = getPluginLoader({ debug: true });

// Subscribe to events
const unsubscribe = loader.subscribe((event) => {
  console.log('Plugin event:', event.type);
});

// Load all plugins
const plugins = await loader.loadAll();

// Get widgets for dashboard
const dashboardWidgets = loader.getWidgetsForSlot('dashboard', userCapabilities);

// Lazy load a widget component
const component = await loader.loadWidgetComponent('bolt:command-executor');
```

#### Step 16: Build Widget Registry ✅ COMPLETED

**Status:** Completed 2026-01-31

**File:** `frontend/src/lib/plugins/WidgetRegistry.svelte.ts`

**Actions Completed:**

- ✅ Created centralized widget registry with Svelte 5 reactive state (`$state()`)
- ✅ Implemented permission-based widget filtering with wildcard capability matching
- ✅ Support slot-based widget retrieval with `getWidgetsForSlot()`
- ✅ Advanced filtering with `getWidgets()` supporting:
  - Filter by slot, plugin name, size, loaded state
  - User capability filtering
  - Capability pattern matching (glob-like)
- ✅ Widget sorting with `getWidgetsSorted()` by priority, name, or plugin
- ✅ Widget registration/unregistration with automatic index management
- ✅ Plugin loader integration with auto-sync on plugin events
- ✅ Event system for registry lifecycle (registered, unregistered, updated, refreshed, cleared)
- ✅ Reactive helpers for Svelte components:
  - `useSlotWidgets(slot, getUserCapabilities)` - Reactive widget list for a slot
  - `usePluginWidgets(pluginName)` - Reactive widget list for a plugin
  - `useWidget(widgetId)` - Reactive single widget
- ✅ Singleton pattern with `getWidgetRegistry()` / `resetWidgetRegistry()`
- ✅ Capability wildcard matching (e.g., "bolt.*" matches "bolt.command")
- ✅ Updated barrel exports in `frontend/src/lib/plugins/index.ts`

**Types Exported:**

```typescript
export interface WidgetFilterOptions {
  slot?: WidgetSlot;
  pluginName?: string;
  size?: WidgetSize;
  userCapabilities?: string[];
  loadedOnly?: boolean;
  capabilityPattern?: string;
  tags?: string[];
}

export interface WidgetSortOptions {
  by: "priority" | "name" | "pluginName";
  direction: "asc" | "desc";
}

export type WidgetRegistryEvent =
  | { type: "widget:registered"; widgetId: string; widget: LoadedWidget }
  | { type: "widget:unregistered"; widgetId: string }
  | { type: "widget:updated"; widgetId: string; widget: LoadedWidget }
  | { type: "registry:refreshed"; widgetCount: number }
  | { type: "registry:cleared" }
  | { type: "registry:error"; error: string };
```

**Usage Example:**

```typescript
import { getWidgetRegistry, useSlotWidgets } from '$lib/plugins';

// Get the widget registry
const registry = getWidgetRegistry();

// Subscribe to events
const unsubscribe = registry.subscribe((event) => {
  console.log('Registry event:', event.type);
});

// Refresh from plugin loader
registry.refresh();

// Get widgets for a slot with permission filtering
const dashboardWidgets = registry.getWidgetsForSlot('dashboard', userCapabilities);

// Advanced filtering
const widgets = registry.getWidgets({
  slot: 'dashboard',
  userCapabilities: ['bolt.*', 'inventory.read'],
  loadedOnly: true,
});

// Check if user can view a widget
const canView = registry.canViewWidget('bolt:command-executor', userCapabilities);

// In Svelte component
const widgets = useSlotWidgets('dashboard', () => userCapabilities);
```

#### Step 17: Create Widget Slot Component ✅ COMPLETED

**Status:** Completed 2026-01-31

**File:** `frontend/src/lib/plugins/WidgetSlot.svelte`

**Actions Completed:**

- ✅ Created `WidgetSlot.svelte` component for rendering widgets in designated slots
- ✅ Implemented three layout modes:
  - **Grid layout**: Responsive grid with configurable columns (1-4), widget size-aware spanning
  - **Stack layout**: Vertical stacking of widgets with consistent styling
  - **Tabs layout**: Tabbed interface with tab switching and lazy content loading
- ✅ Lazy loading of widget components with reactive state tracking
- ✅ Permission-based widget filtering via `userCapabilities` prop
- ✅ Error handling with retry capability for failed widget loads
- ✅ Loading states with skeleton animations and spinners
- ✅ Empty state display when no widgets available
- ✅ Debug mode panel showing widget states and metadata
- ✅ Widget size classes for grid layout (`small`, `medium`, `large`, `full`)
- ✅ Responsive design with Tailwind CSS
- ✅ Dark mode support
- ✅ Updated barrel exports in `frontend/src/lib/plugins/index.ts`

**Component Props:**

```typescript
interface Props {
  slot: WidgetSlotType;           // Slot for widget filtering
  layout?: "grid" | "stack" | "tabs"; // Layout mode (default: "grid")
  userCapabilities?: string[];    // Permissions for filtering
  columns?: 1 | 2 | 3 | 4;       // Grid columns (default: 2)
  gap?: "2" | "4" | "6" | "8";   // Gap between widgets (default: "4")
  showEmptyState?: boolean;       // Show empty state (default: true)
  emptyMessage?: string;          // Custom empty message
  debug?: boolean;                // Enable debug panel
  class?: string;                 // Additional CSS classes
}
```

**Usage Example:**

```svelte
<script>
  import { WidgetSlot } from '$lib/plugins';
  let userCapabilities = ['bolt.*', 'inventory.read'];
</script>

<!-- Grid layout with 3 columns -->
<WidgetSlot
  slot="dashboard"
  layout="grid"
  columns={3}
  {userCapabilities}
/>

<!-- Stack layout -->
<WidgetSlot
  slot="node-detail"
  layout="stack"
  {userCapabilities}
/>

<!-- Tabs layout -->
<WidgetSlot
  slot="inventory-panel"
  layout="tabs"
  {userCapabilities}
/>
```

#### Step 18: Implement Debug Mode ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `frontend/src/lib/debug/debugMode.svelte.ts` - Debug mode store with request tracking
- ✅ `frontend/src/lib/debug/useDebugContext.ts` - Per-widget debug context hooks
- ✅ `frontend/src/lib/debug/index.ts` - Barrel exports
- ✅ `frontend/src/components/DebugPanel.svelte` - Debug panel component
- ✅ Updated `frontend/src/lib/expertMode.svelte.ts` - Now re-exports from debug module

**Actions Completed:**

- ✅ Renamed expert mode to `debugMode` (with backward-compatible `expertMode` alias)
- ✅ Created debug context system per widget with `useDebugContext()` hook
- ✅ Implemented API request tracking with correlation IDs
- ✅ Added `trackedFetch()` utility for automatic request tracking
- ✅ Created `DebugPanel.svelte` component with:
  - Compact and full display modes
  - Request list with status, timing, and size
  - Statistics tab (total, pending, successful, failed, avg duration)
  - Configuration tab for debug settings
- ✅ Maintained full backward compatibility with existing `expertMode` usage

**Key Features:**

- **DebugModeStore**: Enhanced state management with:
  - `enabled` state (localStorage persisted)
  - `config` for debug settings (logApiRequests, includePerformance, etc.)
  - `requests` tracked API requests array
  - Request tracking methods (`trackRequestStart`, `trackRequestEnd`)
  - Debug context management (`createContext`, `getContext`, `destroyContext`)
  - Request statistics (`getRequestStats`)

- **useDebugContext()**: Svelte hook for per-widget debug contexts:
  - Automatic correlation ID generation
  - Scoped request tracking
  - Custom debug data storage
  - Child context support for nested widgets
  - Automatic cleanup on component destroy
  - Logging helpers (`log`, `warn`, `error`)
  - HTTP header generation for correlation

- **DebugPanel.svelte**: Visual debug information display:
  - Compact inline mode with summary badges
  - Full panel with tabs (Requests, Stats, Config)
  - Request filtering by correlation ID
  - Backend debug info integration
  - Copy to clipboard functionality

**Migration Notes:**

Existing code using `expertMode` continues to work without changes:

```typescript
// Old way (still works)
import { expertMode } from '$lib/expertMode.svelte';

// New way (recommended)
import { debugMode, useDebugContext } from '$lib/debug';
```

#### Step 19: Build Authentication Store ✅ COMPLETED

**Status:** Completed 2026-01-31

**File:** `frontend/src/lib/auth.svelte.ts`

**Actions Completed:**

- ✅ Implemented `AuthStore` class with Svelte 5 reactive state (`$state()`)
- ✅ Login functionality:
  - `login(credentials)` - Authenticate with username/password
  - Calls `/api/auth/login` endpoint
  - Stores tokens, user, and permissions
  - Emits `login` event to subscribers
- ✅ Logout functionality:
  - `logout(reason?)` - Clear authentication state
  - Calls `/api/auth/logout` endpoint
  - Clears tokens from localStorage
  - Emits `logout` event to subscribers
  - `logoutAllSessions()` - Logout from all devices
- ✅ Token refresh:
  - `refreshTokens()` - Refresh access token using refresh token
  - Automatic refresh 5 minutes before expiry via timer
  - Handles refresh failures with logout
  - Emits `token_refresh` event
- ✅ User and permissions storage:
  - `user` - Current authenticated user
  - `permissions` - User's allowed/denied capabilities and roles
  - `fetchPermissions()` - Refresh permissions from `/api/auth/me`
  - localStorage persistence with expiration tracking
- ✅ Reactive permission checking:
  - `hasCapability(capability)` - Check single capability
  - `hasAnyCapability(capabilities)` - Check any of multiple capabilities
  - `hasAllCapabilities(capabilities)` - Check all capabilities
  - `hasRole(role)` / `hasAnyRole(roles)` - Role checks
  - `isAdmin` - Computed admin check
  - Wildcard pattern matching (`*`, `category.*`)
- ✅ Server-side permission check:
  - `checkCapability(capability, context?)` - Check with conditions
- ✅ Reactive helpers for Svelte components:
  - `useCapability(capability)` - Reactive capability check
  - `useAnyCapability(capabilities)` - Reactive any capability check
  - `useAllCapabilities(capabilities)` - Reactive all capabilities check
  - `useRole(role)` - Reactive role check
  - `useIsAuthenticated()` - Reactive auth state
  - `useUser()` - Reactive user data
  - `useIsAdmin()` - Reactive admin check
- ✅ Event subscription system for auth lifecycle events
- ✅ Singleton pattern with `getAuthStore()` / `resetAuthStore()`
- ✅ `getAuthHeader()` - Get Authorization header for API requests

**Key Types Exported:**

```typescript
export interface AuthTokens {
  accessToken: string;
  refreshToken: string;
  tokenType: "Bearer";
  expiresIn: number;
  refreshExpiresIn: number;
}

export interface User {
  id: string;
  username: string;
  email: string;
  displayName?: string;
  groups: string[];
  roles: string[];
  active: boolean;
}

export interface UserPermissions {
  allowed: string[];   // Allowed capability patterns
  denied: string[];    // Denied capability patterns
  roles: string[];     // Role names
}

export type AuthEvent =
  | { type: "login"; user: User }
  | { type: "logout"; reason?: string }
  | { type: "token_refresh"; success: boolean }
  | { type: "permission_change"; permissions: UserPermissions }
  | { type: "error"; error: string };
```

**Usage Example:**

```svelte
<script>
  import { auth, useCapability, useIsAuthenticated } from '$lib/auth.svelte';

  const isAuthenticated = useIsAuthenticated();
  const canExecuteCommands = useCapability('command.execute');

  async function handleLogin() {
    const success = await auth.login({ username: 'admin', password: 'secret' }); // pragma: allowlist secret
    if (success) {
      console.log('Logged in as:', auth.user?.username);
    }
  }
</script>

{#if isAuthenticated.value}
  <p>Welcome, {auth.user?.displayName || auth.user?.username}!</p>
  {#if canExecuteCommands.value}
    <button>Execute Command</button>
  {/if}
  <button onclick={() => auth.logout()}>Logout</button>
{:else}
  <button onclick={handleLogin}>Login</button>
{/if}
```

#### Step 20: Create Permission Guard Component ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `frontend/src/components/PermissionGuard.svelte` - Main permission guard component
- ✅ `frontend/src/components/RequireAuth.svelte` - Authentication-only wrapper
- ✅ `frontend/src/components/AdminOnly.svelte` - Admin role wrapper
- ✅ `frontend/src/components/PermissionGuard.example.md` - Usage documentation
- ✅ Updated `frontend/src/components/index.ts` - Barrel exports

**Actions Completed:**

- ✅ Created `PermissionGuard.svelte` with comprehensive features:
  - Single capability check (`capability` prop)
  - Multiple capabilities check (`capabilities` prop with `mode="any"|"all"`)
  - Role-based checks (`role` and `roles` props)
  - Custom fallback content via snippet
  - Loading state with customizable content
  - Silent mode (no fallback shown)
  - Invert mode (show when permission denied)
  - Integration with auth store's reactive permission checking

- ✅ Created `RequireAuth.svelte` as simplified auth wrapper:
  - Authentication-only checks (no capability/role)
  - Redirect to login page support
  - Return URL preservation for post-login redirect
  - Custom fallback content
  - Default sign-in prompt with button

- ✅ Created `AdminOnly.svelte` as admin role wrapper:
  - Simplified admin role check
  - Amber-styled default fallback for admin content
  - Delegates to PermissionGuard internally

- ✅ Updated barrel exports in `frontend/src/components/index.ts`

- ✅ Created usage examples documentation

**Component API Summary:**

```typescript
// PermissionGuard props
interface PermissionGuardProps {
  capability?: string;           // Single capability to check
  capabilities?: string[];       // Multiple capabilities
  mode?: "any" | "all";         // Check mode for multiple
  role?: string;                // Single role to check
  roles?: string[];             // Multiple roles
  requireAuth?: boolean;        // Require authentication (default: true)
  showLoading?: boolean;        // Show loading state (default: true)
  silent?: boolean;             // Don't show fallback (default: false)
  loadingText?: string;         // Custom loading text
  invert?: boolean;             // Invert the check
  children: Snippet;            // Content when permitted
  fallback?: Snippet;           // Content when denied
  loading?: Snippet;            // Custom loading content
}

// RequireAuth props
interface RequireAuthProps {
  redirectTo?: string;          // Login redirect URL
  showLoading?: boolean;
  loadingText?: string;
  silent?: boolean;
  children: Snippet;
  fallback?: Snippet;
  loading?: Snippet;
}

// AdminOnly props
interface AdminOnlyProps {
  showLoading?: boolean;
  silent?: boolean;
  children: Snippet;
  fallback?: Snippet;
  loading?: Snippet;
}
```

**Usage Examples:**

```svelte
<!-- Basic capability check -->
<PermissionGuard capability="command.execute">
  <CommandExecutor />
</PermissionGuard>

<!-- Multiple capabilities (any) -->
<PermissionGuard capabilities={["bolt.command", "bolt.task"]} mode="any">
  <BoltInterface />
</PermissionGuard>

<!-- With custom fallback -->
<PermissionGuard capability="admin.settings">
  <AdminPanel />
  {#snippet fallback()}
    <p>Admin access required.</p>
  {/snippet}
</PermissionGuard>

<!-- Auth-only check with redirect -->
<RequireAuth redirectTo="/login">
  <Dashboard />
</RequireAuth>

<!-- Admin-only content -->
<AdminOnly>
  <SystemSettings />
</AdminOnly>
```

**File:** `frontend/src/components/PermissionGuard.svelte`

#### Step 21: Build Dynamic Menu System ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created:**

- ✅ `frontend/src/lib/navigation/types.ts` - Type definitions for menu system
- ✅ `frontend/src/lib/navigation/MenuBuilder.svelte.ts` - Menu builder with Svelte 5 reactivity
- ✅ `frontend/src/lib/navigation/index.ts` - Barrel exports
- ✅ `frontend/src/lib/navigation/MenuBuilder.test.ts` - Unit tests (29 tests)
- ✅ `frontend/src/components/DynamicNavigation.svelte` - Navigation component using MenuBuilder

**Actions Completed:**

- ✅ Created comprehensive type definitions for menu system:
  - `MenuItem`, `LinkMenuItem`, `GroupMenuItem`, `DividerMenuItem`, `ActionMenuItem`
  - `MenuSection`, `Menu`, `PluginMenuContribution`
  - `MenuBuilderConfig`, `MenuBuilderEvent`, `MenuBuilderEventHandler`
  - `IntegrationTypeMetadata` with default metadata for all integration types

- ✅ Implemented `MenuBuilder` class with:
  - Plugin loader integration (subscribes to plugin events)
  - Automatic menu generation from loaded plugins
  - Permission-based filtering via auth store
  - Integration type grouping with configurable nesting
  - Priority-based ordering for sections, groups, and items
  - Custom contribution support (preserved across rebuilds)
  - Event subscription system for menu lifecycle events
  - Svelte 5 reactive state (`$state()`) for menu updates

- ✅ Core navigation items:
  - Home, Inventory, Executions (core section)
  - Users, Roles, Plugins, Settings (admin section)

- ✅ Plugin contribution handling:
  - Automatic menu item generation from plugin capabilities
  - Category-based grouping within plugins
  - Separate storage for plugin vs custom contributions
  - Custom contributions preserved across menu rebuilds

- ✅ Created `DynamicNavigation.svelte` component:
  - Uses MenuBuilder for dynamic menu rendering
  - Dropdown menus for grouped items
  - Active route highlighting
  - Integration type color coding
  - Theme and expert mode toggles
  - User info and logout button
  - Loading and error states

- ✅ Reactive helpers:
  - `useMenu()` - Reactive access to current menu
  - `useMenuSection(sectionId)` - Reactive access to specific section
  - `useMenuIsBuilding()` - Reactive building state

- ✅ Singleton pattern with `getMenuBuilder()` / `resetMenuBuilder()`

**Test Results:** 29 tests passing

**Configuration Options:**

```typescript
interface MenuBuilderConfig {
  includeCoreItems?: boolean;        // Include Home, etc. (default: true)
  filterByPermissions?: boolean;     // Filter by user capabilities (default: true)
  sortByPriority?: boolean;          // Sort items by priority (default: true)
  groupByIntegrationType?: boolean;  // Group plugins by type (default: true)
  showEmptyGroups?: boolean;         // Show groups without items (default: false)
  defaultPluginSection?: string;     // Section for plugin items (default: "integrations")
  sections?: MenuSection[];          // Custom section definitions
}
```

**Usage Example:**

```typescript
import { getMenuBuilder, useMenu } from '$lib/navigation';

// Get the menu builder (singleton)
const menuBuilder = getMenuBuilder();

// Subscribe to events
const unsubscribe = menuBuilder.subscribe((event) => {
  if (event.type === 'menu:built') {
    console.log('Menu built with', event.menu.metadata?.itemCount, 'items');
  }
});

// Initialize (subscribes to plugin loader)
menuBuilder.initialize();

// In Svelte component, use reactive helpers
const menu = useMenu();
// Access via menu.value
```

#### Step 22: Update Page Components ✅ COMPLETED

**Status:** Completed 2026-01-31

**Files Created/Updated:**

- ✅ `frontend/src/pages/HomePage.svelte` - Updated with dashboard widget slots
- ✅ `frontend/src/pages/InventoryPage.svelte` - Updated with inventory-panel widget slot
- ✅ `frontend/src/pages/PluginManagerPage.svelte` - New plugin management UI
- ✅ `frontend/src/pages/LoginPage.svelte` - New login page
- ✅ `frontend/src/App.svelte` - Added new routes

**Actions Completed:**

- ✅ Added widget slots to HomePage:
  - Dashboard slot with grid layout for plugin widgets
  - Sidebar slot with stack layout for quick actions
  - Integration with `WidgetSlot` component
  - User capability filtering via auth store

- ✅ Added widget slots to InventoryPage:
  - Inventory-panel slot with tabs layout
  - Integration with existing expert mode debug panel
  - User capability filtering

- ✅ Created PluginManagerPage with:
  - Plugin list with search and filtering (enabled/disabled)
  - Stats cards (total, enabled, healthy, with widgets, capabilities)
  - Plugin details panel with tabs (Overview, Capabilities, Widgets)
  - Plugin reload functionality
  - Admin-only access via `AdminOnly` component
  - Expert mode debug info support

- ✅ Created LoginPage with:
  - Username/password login form
  - Error handling and validation states
  - Remember me checkbox
  - Redirect after successful login (via returnTo query param)
  - Dark mode support
  - Responsive design

- ✅ Updated App.svelte routes:
  - `/login` → LoginPage
  - `/plugins` → PluginManagerPage

### Phase 5: Integration Migration & Legacy Cleanup (Week 5-6) ⚡ PRIORITY

> **Note:** This phase was moved up from Week 8. The priority is to migrate existing integrations to the new modular plugin architecture and remove legacy code before proceeding with CLI development.

#### Step 23: Migrate Bolt Plugin

**Files:**

- `backend/src/integrations/bolt/BoltPlugin.ts` → Refactor to new plugin format
- `backend/src/integrations/bolt/index.ts` → New plugin entry point

**Current Code to Keep:**

- Core Bolt CLI execution logic (spawn, streaming, timeout handling)
- Inventory parsing (YAML inventory file support)
- Command/task execution with target resolution
- Caching logic (inventory TTL, facts TTL)

**Code to Remove/Refactor:**

- Old `ExecutionToolPlugin` interface implementation
- Legacy debug info attachment patterns
- Direct route coupling

**Actions:**

- ✅ Implement `BasePluginInterface` with proper metadata
- ✅ Register capabilities: `bolt.command`, `bolt.task`, `bolt.inventory`, `bolt.facts`
- ✅ Define widgets: CommandExecutor, TaskRunner, InventoryViewer
- ✅ Add capability handlers with proper RBAC context
- Update tests to use new plugin interface

#### Step 24: Migrate PuppetDB Plugin

**Files:**

- `backend/src/integrations/puppetdb/PuppetDBService.ts` → Keep core logic
- `backend/src/integrations/puppetdb/PuppetDBClient.ts` → Keep HTTP client
- `backend/src/integrations/puppetdb/CircuitBreaker.ts` → Keep resilience patterns
- `backend/src/integrations/puppetdb/index.ts` → New plugin entry point

**Current Code to Keep:**

- PQL query execution
- Node/fact/report fetching
- Circuit breaker and retry logic
- SSL/TLS configuration

**Code to Remove/Refactor:**

- Old `InformationSourcePlugin` interface
- Legacy expert mode patterns
- Redundant type exports

**Actions:**

- Implement `BasePluginInterface` with PuppetDB metadata
- Register capabilities: `puppetdb.query`, `puppetdb.nodes`, `puppetdb.facts`, `puppetdb.reports`, `puppetdb.events`
- Define widgets: NodeBrowser, FactsExplorer, ReportsViewer, PQLQueryBuilder
- Maintain circuit breaker for reliability
- Update tests

#### Step 25: Migrate Puppetserver Plugin

**Files:**

- `backend/src/integrations/puppetserver/PuppetserverService.ts` → Keep core logic
- `backend/src/integrations/puppetserver/PuppetserverClient.ts` → Keep HTTP client
- `backend/src/integrations/puppetserver/index.ts` → New plugin entry point

**Current Code to Keep:**

- Catalog compilation requests
- Environment listing
- Node classification (if present)
- SSL/certificate handling

**Code to Remove/Refactor:**

- Old plugin interface implementation
- Certificate management code (removed in v0.4)
- Legacy debug patterns

**Actions:**

- Implement `BasePluginInterface` with Puppetserver metadata
- Register capabilities: `puppetserver.catalog`, `puppetserver.environments`, `puppetserver.status`
- Define widgets: CatalogViewer, EnvironmentSelector
- Update tests

#### Step 26: Migrate Hiera Plugin

**Files:**

- `backend/src/integrations/hiera/HieraService.ts` → Keep core logic
- `backend/src/integrations/hiera/HieraResolver.ts` → Keep resolution logic
- `backend/src/integrations/hiera/HieraParser.ts` → Keep parsing
- `backend/src/integrations/hiera/HieraScanner.ts` → Keep file scanning
- `backend/src/integrations/hiera/index.ts` → New plugin entry point

**Current Code to Keep:**

- Hiera hierarchy resolution
- YAML/JSON/EYAML parsing
- Key lookup with interpolation
- File scanning and caching

**Code to Consider Removing:**

- `CatalogCompiler.ts` - Evaluate if still needed
- `CodeAnalyzer.ts` - Evaluate if still needed
- `ForgeClient.ts` - Move to separate Forge plugin if needed
- `PuppetfileParser.ts` - Move to separate module management plugin

**Actions:**

- Implement `BasePluginInterface` with Hiera metadata
- Register capabilities: `hiera.lookup`, `hiera.keys`, `hiera.hierarchy`, `hiera.scan`
- Define widgets: HieraExplorer, KeyLookup, HierarchyViewer
- Declare optional PuppetDB dependency for node facts
- Update tests

#### Step 27: Legacy Code Cleanup

**Backend Files to Remove/Consolidate:**

```
backend/src/integrations/
├── ApiLogger.ts           → Remove (replaced by LoggerService)
├── NodeLinkingService.ts  → Keep (multi-source node linking is valuable)
└── BasePlugin.ts          → Update to new interface only

backend/src/services/
├── ExpertModeService.ts   → Remove (replaced by debug mode in middleware)
└── StreamingExecutionManager.ts → Keep (core streaming functionality)

backend/src/middleware/
├── expertMode.ts          → Remove (replaced by debug middleware)
└── debugInfo.ts           → Consolidate into single debug middleware
```

**Frontend Files to Consolidate:**

```
frontend/src/lib/
├── expertMode.svelte.ts   → Already aliased to debugMode, keep alias
└── debug/                 → Primary debug implementation

frontend/src/components/
├── ExpertModeDebugPanel.svelte  → Keep as compatibility alias
├── ExpertModeCopyButton.svelte  → Keep as compatibility alias
└── DebugPanel.svelte            → Primary component
```

**Actions:**

- Remove deprecated `ApiLogger.ts` (use `LoggerService` instead)
- Consolidate expert mode → debug mode throughout codebase
- Remove unused route-level debug info attachment code
- Update imports across all files
- Run full test suite to verify no regressions

#### Step 28: Route Refactoring for Capability-Based Access

**Current Routes to Refactor:**

```
backend/src/routes/
├── commands.ts      → Use executeCapability('bolt.command', ...)
├── tasks.ts         → Use executeCapability('bolt.task', ...)
├── inventory.ts     → Use executeCapability('inventory.list', ...)
├── facts.ts         → Use executeCapability('*.facts', ...)
├── puppet.ts        → Use executeCapability('puppetserver.*', ...)
├── hiera.ts         → Use executeCapability('hiera.*', ...)
└── puppetdb.ts      → Use executeCapability('puppetdb.*', ...)
```

**Actions:**

- Update routes to use `IntegrationManager.executeCapability()`
- Remove direct plugin method calls
- Add RBAC middleware checks (`requireCapability()`)
- Ensure consistent error handling via capability system
- Update API documentation

#### Step 29: Frontend Widget Integration

**Actions:**

- Create actual widget components for each plugin
- Register widgets with WidgetRegistry on plugin load
- Update HomePage dashboard to render plugin widgets
- Update InventoryPage to use inventory widgets
- Update NodeDetailPage with node-specific widgets
- Remove hardcoded integration-specific components where replaced by widgets

### Phase 6: Testing & Validation (Week 7)

#### Step 30: Comprehensive Integration Testing

**Actions:**

- Unit tests for all migrated plugins
- Integration tests for capability execution
- E2E tests for Web UI with new widget system
- RBAC enforcement tests across all interfaces
- Performance tests for plugin loading
- Regression tests for existing functionality

#### Step 31: Documentation Updates

**Files:**

- `docs/plugin-development.md` - How to create plugins
- `docs/migration-guide.md` - 0.x → 1.0 migration
- `docs/architecture.md` - Update with new plugin system
- `docs/api.md` - Update endpoint documentation
- `CHANGELOG.md` - Document all changes

### Phase 7: CLI Application (Week 8) - DEFERRED

> **Note:** CLI development is deferred until integration migration is complete and stable. The web UI and API provide full functionality.

#### Step 32: Create CLI Workspace Structure

**Actions:**

- Add CLI workspace to root package.json
- Create cli/package.json with dependencies
- Set up TypeScript configuration

#### Step 33: Build CLI Configuration

**File:** `cli/src/config/CliConfig.ts`

**Actions:**

- Use `conf` package for `~/.pabawirc` management
- Store server URL, tokens, preferences
- Support multiple configuration profiles

#### Step 34: Create API Client

**Files:**

- `cli/src/client/ApiClient.ts`
- `cli/src/client/AuthClient.ts`

**Actions:**

- REST API wrapper with authentication
- Auto-refresh expired tokens
- Handle 401/403 responses

#### Step 35: Implement Command Registry

**File:** `cli/src/commands/CommandRegistry.ts`

**Actions:**

- Fetch plugin metadata from server
- Dynamically generate commands from capabilities
- Add arguments from capability schemas
- Route to capability execution

#### Step 36: Create Core Commands

**File:** `cli/src/commands/CoreCommands.ts`

**Actions:**

- `pab login` - Interactive login
- `pab logout` - Clear credentials
- `pab whoami` - Show current user
- `pab config` - Manage configuration
- `pab plugins` - List/show plugins
- `pab capabilities` - List capabilities

#### Step 37: Implement Output Formatters

**File:** `cli/src/formatters/OutputFormatter.ts`

**Actions:**

- Table formatter (cli-table3)
- JSON formatter (pretty-print)
- YAML formatter
- Human-readable formatter

#### Step 38: Build Main CLI Entry Point

**File:** `cli/src/index.ts`

**Actions:**

- Shebang for executable
- Load core commands
- Dynamically load plugin commands
- Handle global options (--debug)

#### Step 39: Add Shell Completions

**Files:**

- `cli/completions/pab.bash`
- `cli/completions/pab.zsh`
- `cli/completions/pab.fish`

**Actions:**

- Generate completion scripts
- Add `pab completion` command
- Installation instructions

### Phase 8: Documentation & Polish (Week 9)

#### Step 40: Final Documentation

**Files:**

- `docs/plugin-development.md`
- `docs/plugin-frontend-development.md`
- `docs/cli.md`
- `docs/rbac.md`
- `docs/database.md`
- `docs/configuration.md` (update)
- `docs/migration-guide.md` (0.x → 1.0)

#### Step 41: Create Migration Scripts

**File:** `scripts/migrate-to-v1.sh`

**Actions:**

- Convert env vars to YAML
- Create default admin user
- Backup existing data
- Migrate database schema

#### Step 42: Update Build & CI/CD

**Actions:**

- Update GitHub Actions workflows
- Add CLI build/test steps
- Update Docker images to include CLI
- Multi-arch builds
- Database adapter testing

#### Step 43: Example External Plugin

**Directory:** `examples/@pabawi-plugins/ansible/`

**Purpose:** Demonstrate third-party plugin development

**Contents:**

- Complete plugin with backend + frontend + CLI
- Documentation
- Tests
- Support multiple configuration profiles

#### Step 25: Create API Client

**Files:**

- `cli/src/client/ApiClient.ts`
- `cli/src/client/AuthClient.ts`

**Actions:**

- REST API wrapper with authentication
- Auto-refresh expired tokens
- Handle 401/403 responses

#### Step 26: Implement Command Registry

**File:** `cli/src/commands/CommandRegistry.ts`

**Actions:**

- Fetch plugin metadata from server
- Dynamically generate commands from capabilities
- Add arguments from capability schemas
- Route to capability execution

#### Step 27: Create Core Commands

**File:** `cli/src/commands/CoreCommands.ts`

**Actions:**

- `pab login` - Interactive login
- `pab logout` - Clear credentials
- `pab whoami` - Show current user
- `pab config` - Manage configuration
- `pab plugins` - List/show plugins
- `pab capabilities` - List capabilities

#### Step 28: Implement Output Formatters

**File:** `cli/src/formatters/OutputFormatter.ts`

**Actions:**

- Table formatter (cli-table3)
- JSON formatter (pretty-print)
- YAML formatter
- Human-readable formatter

#### Step 29: Build Main CLI Entry Point

**File:** `cli/src/index.ts`

**Actions:**

- Shebang for executable
- Load core commands
- Dynamically load plugin commands
- Handle global options (--debug)

#### Step 30: Add Shell Completions

**Files:**

- `cli/completions/pab.bash`
- `cli/completions/pab.zsh`
- `cli/completions/pab.fish`

**Actions:**

- Generate completion scripts
- Add `pab completion` command
- Installation instructions

## Breaking Changes from v0.x

1. **Plugin Interface**: Complete rewrite - all plugins must be updated
2. **Configuration**: Env vars still supported but YAML recommended
3. **Authentication**: Now required (can be disabled via config)
4. **API**: Endpoints refactored to use capability-based routing
5. **Frontend**: Widget system replaces hardcoded integration components
6. **Debug Mode**: Expert mode renamed to debug mode (aliases maintained)
7. **Legacy Code**: ApiLogger, old expert mode services removed
8. **CLI**: New CLI replaces any previous scripts (deferred to Phase 7)
9. **Database**: Abstracted - SQLite still default but prepared for alternatives

## Code Cleanup Summary

### Files to Remove

| File | Reason |
|------|--------|
| `backend/src/integrations/ApiLogger.ts` | Replaced by LoggerService |
| `backend/src/services/ExpertModeService.ts` | Replaced by debug middleware |
| `backend/src/middleware/expertMode.ts` | Consolidated into debug middleware |

### Files to Consolidate

| Old Location | New Location | Notes |
|--------------|--------------|-------|
| `expertMode.svelte.ts` | `debug/debugMode.svelte.ts` | Keep alias for compatibility |
| `ExpertModeDebugPanel.svelte` | `DebugPanel.svelte` | Keep component, update name |
| Multiple debug info handlers | Single debug middleware | Centralize debug context |

### Integration Code Focus

For each integration, keep only:

1. **Core Service Logic** - Actual API/CLI interaction code
2. **Client Classes** - HTTP/process communication
3. **Type Definitions** - Interface contracts
4. **Resilience Patterns** - Circuit breakers, retry logic, caching

Remove:

- Old plugin interface implementations
- Legacy debug/expert mode patterns
- Redundant exports and wrappers
- Unused utility code

## Migration Path

1. Backup existing Pabawi installation
2. Install v1.0.0
3. Run migration script to convert config
4. Create admin user
5. Test authentication
6. Verify plugins loaded
7. Test database migration
8. Update custom integrations (if any)

## Rollback Plan

Keep v0.x backup available. If issues arise:

1. Stop v1.0.0 server
2. Restore v0.x code and data
3. Restart v0.x server

## Database Strategy

### Current Implementation (v1.0.0)

- **SQLite**: Default and primary database
- **Adapter Pattern**: All database code abstracted behind `DatabaseAdapter` interface
- **Migration Support**: Version-tracked migrations work across adapters

### Future Database Support (v1.1+)

The abstraction layer enables adding:

1. **PostgreSQL**: For production deployments with high concurrency
   - Connection pooling
   - Better concurrent write performance
   - Advanced features (JSONB, full-text search)

2. **MySQL/MariaDB**: For organizations standardized on MySQL
   - Wide deployment support
   - Cloud provider compatibility

3. **Migration Path**:
   - Export data from SQLite
   - Initialize new database with schema
   - Import data via adapter
   - Update configuration

### Database Adapter Implementation Checklist

When adding a new database adapter:

- [ ] Implement `DatabaseAdapter` interface
- [ ] Handle dialect-specific SQL syntax
- [ ] Implement connection pooling
- [ ] Support transactions properly
- [ ] Write migration compatibility layer
- [ ] Add comprehensive tests
- [ ] Document configuration options
- [ ] Update factory to support new type

## CLI Synopsis Examples (Phase 7 - Deferred)

> These CLI commands will be available after Phase 7 is complete.

```bash
# Core commands
pab login
pab logout  
pab whoami
pab config set output.format json
pab plugins list
pab capabilities

# Plugin commands (auto-generated from bolt plugin)
pab bolt run "uptime" --targets all
pab bolt task package --targets web-* --params '{"action":"install","name":"nginx"}'
pab bolt inventory

# Plugin commands (auto-generated from puppetdb plugin)
pab puppetdb facts web-01
pab puppetdb query "nodes { certname ~ 'web' }"
pab puppetdb reports --status failed --since 1h

# Plugin commands (auto-generated from hiera plugin)
pab hiera lookup ntp_servers --node web-01 --environment production
pab hiera keys --pattern '*password*'

# Debug mode
pab --debug bolt run "systemctl status nginx" --targets web-01

# Different output formats
pab bolt inventory --format table
pab bolt inventory --format json | jq '.[] | .certname'

# RBAC enforcement (user with viewer role)
$ pab bolt run "reboot" --targets all
✗ Permission denied: You lack the 'command.execute' capability.
```

## Success Metrics

- All existing integrations (Bolt, PuppetDB, Puppetserver, Hiera) migrated to new plugin architecture
- Legacy debug/expert mode code consolidated
- Routes refactored to use capability-based access
- RBAC functioning across Web UI and API
- Widget system rendering plugin widgets on pages
- Database abstraction layer complete with SQLite adapter
- Documentation complete
- All tests passing
- Migration from v0.x successful
- CLI functional (Phase 7, deferred)

## Timeline (Revised)

- **Weeks 1-2:** Core plugin infrastructure + database abstraction ✅
- **Week 3:** Authentication & authorization ✅
- **Week 4:** Configuration system ✅
- **Weeks 5-6:** **Integration migration & legacy cleanup** ⚡ CURRENT PRIORITY
- **Week 7:** Testing & validation
- **Week 8:** CLI application (deferred)
- **Week 9:** Documentation & polish
- **Total:** 9 weeks to v1.0.0 release

## Next Steps (Immediate)

1. **Step 23:** Migrate Bolt Plugin to new `BasePluginInterface`
2. **Step 24:** Migrate PuppetDB Plugin
3. **Step 25:** Migrate Puppetserver Plugin
4. **Step 26:** Migrate Hiera Plugin
5. **Step 27:** Remove legacy code (ApiLogger, old expert mode)
6. **Step 28:** Refactor routes to use capability-based access
7. **Step 29:** Create and register actual frontend widgets

## Future Considerations (v1.1+)

1. **Plugin Marketplace**: Registry for third-party plugins
2. **Hot Reload**: Reload plugins without server restart (production-safe)
3. **Multi-Tenancy**: Organization/tenant support
4. **SSO/LDAP**: Enterprise authentication
5. **Service Accounts**: API tokens for automation
6. **Widget Configuration UI**: Drag-and-drop dashboard builder
7. **Plugin Sandboxing**: Security isolation for untrusted plugins
8. **CLI Server Profiles**: Manage multiple Pabawi servers
9. **Streaming Output**: Real-time command output in CLI
10. **Plugin Dependencies**: Automatic dependency installation
11. **PostgreSQL Adapter**: Production-ready database backend
12. **MySQL Adapter**: Alternative database option
13. **Database Migration Tools**: GUI for database upgrades
14. **Multi-Database Support**: Read replicas, sharding

## Notes

### Database Abstraction Rationale

The database abstraction layer provides:

- **Flexibility**: Easy to switch databases based on deployment needs
- **Testing**: Mock adapters for unit tests without actual database
- **Performance**: Can optimize per-database (e.g., PostgreSQL JSONB for plugin configs)
- **Scalability**: Future support for read replicas, connection pooling strategies
- **Migration**: Smooth path from SQLite (development) to PostgreSQL (production)

SQLite remains the default for its simplicity and zero-configuration setup, making Pabawi easy to deploy for small teams and development. The abstraction ensures larger deployments can migrate to PostgreSQL without code changes.

---

**End of Plan Document**
